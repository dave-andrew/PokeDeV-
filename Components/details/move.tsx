

export default function Move({move}:any){
    return (
        <div style={{
            display: "flex",
            justifyContent: "center",
            alignContent: "center"
        }}>{move}</div>
    )
}