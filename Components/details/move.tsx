

export default function Move({move}){
    return (
        <div style={{
            display: "flex",
            justifyContent: "center",
            alignContent: "center"
        }}>{move}</div>
    )
}